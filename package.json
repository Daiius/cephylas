{
  "name": "cephylas",
  "private": true,
  "scripts": {
    "dev": "docker compose up -d cephylas && pnpm --filter web dev",
    "dev:core": "docker compose up -d cephylas",
    "dev:web": "pnpm --filter web dev",
    "down": "docker compose down",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
